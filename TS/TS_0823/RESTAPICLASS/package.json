{
  "name": "project",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module", 
  "scripts": {
  "build": "tsc",
  "start": "node dist/main.js",
  "dev": "nodemon --watch 'src/**/*.ts' --exec 'npm run build && node dist/main.js'"
},
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "typescript": "^4.3.2",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1"
  }
}
